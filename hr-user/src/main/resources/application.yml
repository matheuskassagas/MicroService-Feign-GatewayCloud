server:
  port: 8003
spring:
  h2:
    console:
      enabled: true
  application:
    name: hr-user

eureka:
  client:
    register-with-eureka:
      fetch-registry:
      service-url:
        defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost